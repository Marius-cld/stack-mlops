services:
  mlflow:
    image: ghcr.io/mlflow/mlflow:v3.16.1
    container_name: mlflow
    ports:
      - "5001:5000"
    command: mlflow server --host 0.0.0.0 --port 5000 --workers 1 --backend-store-uri sqlite:///mlflow/mlflow.db --serve-artifacts --artifacts-destination /mlflow/artifacts --allowed-hosts "localhost:*,127.0.0.1:*,host.docker.internal:*"
    volumes:
      - ./data:/mlflow
    restart: unless-stopped
